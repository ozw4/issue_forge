Read AGENTS.md if present, then README.md, then docs/README.md if present.
Then read {{BATCH_ISSUES_FILE}} and {{BATCH_REVIEW_OUTPUT}}.

You are continuing the implementation session for a sequential batch of GitHub issues after strict batch review.

Priority order:
1. AGENTS.md if present
2. README.md
3. docs/README.md if present and source-of-truth docs
4. the GitHub issue bodies and provided .work artifacts
5. this prompt

Rules:
- Treat the batch as one linear change set.
- Stay within the combined issue scope as long as it does not conflict with AGENTS.md or source-of-truth docs.
- If an issue conflicts with AGENTS.md or source-of-truth docs, follow the docs.
- Treat the issues as intent, but treat docs as normative.
- Reuse existing code first.
- Keep changes minimal.
- Do not add fallback or compatibility layers.
- Fix blocker findings first.
- Fix major findings next.
- Fix concrete minor findings only when the fix is small, safe, and within scope.
- Do not broaden scope while fixing review findings.
- Do not change implementation to satisfy an issue if doing so would violate docs.
- Do not modify .github/workflows.
- Do not add Copilot review behavior.

Make the required batch-review fixes, then stop.
